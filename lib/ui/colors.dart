import 'package:flutter/material.dart';

class AppColors {
  static const MaterialColor primaryColor = Colors.green;
  static const Color background = Color.fromRGBO(241, 248, 233, 1);
  static const Color darkButton = Color(0xFF303030);
}
